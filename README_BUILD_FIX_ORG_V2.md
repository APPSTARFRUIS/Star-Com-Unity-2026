# Build fix Organisation Groupe V2

Correctif TypeScript :
- ajout de l'import `OrgEntity` dans `DocumentsView.tsx`;
- ajout de l'import `OrgEntity` dans `PollsView.tsx`.

Aucune logique fonctionnelle n'a été modifiée.
La migration SQL reste `supabase/organization_group_v2_migration.sql`.
